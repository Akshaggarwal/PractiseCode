#include <stdio.h>
int main() {
	// your code goes here
	int number,i,first=0,second=1,third;
	scanf("%d",&number);
	for(i=1;i<=number;i++){
	    printf("%d \t",first);
	    third  = first+second;
	    first = second;
	    second = third;
	    
	}
}
