#include <stdio.h>
#include<math.h>
int main() {
	// your code goes here
	int year;
	scanf("%d",&year);
	if(((year%4==0)&&(year%100!=0))||year%400==0){
	    printf("%d is a leap year",year);
	}
	else
	printf("it is not a leap year");
	    
}
